import 'package:flutter/material.dart';

import '../constants.dart';

Widget coloredContainer() {
  return Container(
    width: 5,
    height: 40,
    decoration: BoxDecoration(
        color: kOrangeLikeColor, borderRadius: BorderRadius.circular(5)),
  );
}
